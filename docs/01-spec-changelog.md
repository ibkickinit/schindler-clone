# Schindler 2.0 — Spec Changelog

**Purpose:** Dated record of design decisions, architectural pivots, procurement events, and bench milestones for the Schindler 2.0 spec. Entries are kept in the order they were originally recorded so the decision narrative reads naturally — they are not strictly chronological where consecutive sub-updates within the same session were captured slightly out of order.

**Current state lives in [`01-spec.md`](01-spec.md).** This file is read for "when did we decide X" and "why did we change direction on Y" questions.

---

- 2026-05-10 — Initial draft from feature scoping session. V1 confirmed for SDI ref input, composite + component input, all CRT-specific controls, EDID, test pattern set, behavior controls including burn-in repair mode. 60→50 PAL cadence confirmed V1 via crossfade; dev phase uses matched-rate-only fallback until built.
- 2026-05-10 PM — Hardware architecture session. **Confirmed:** TE0720 SOM (Z-7020 industrial) on custom carrier as production target; HD/1080p60 ceiling for v1 with DP via converter chip; HDMI OUT for loop-through monitoring; S-Video OUT (free from ADV7393); SDI as paired IN/OUT broadcast tier with native loop-through (GS3470 + GS2962); WiFi via Laird Sterling LWB5+ with concurrent AP+STA on dual RP-SMA antennas; BLE for setup pairing; UI Pattern B (single TFT + 2 rotary encoders + 4 buttons + status LED column) with hard requirement for physical knob guards; dedicated UI MCU on carrier (STM32H735); STM32H735G-DK as bench dev kit. **Proposed (pending decision):** drop Pi CM4 → Zynq PS hosts web UI; LTC PGA+ADC+AGC front-end with XLR + auto-sensing BNC inputs; ADV7393 as production DAC; GS3470 upgrade from GS2971-class for SDI ref; SKU strategy for broadcast tier (hardware SKU vs firmware unlock); S-Video input addition.
- 2026-05-10 PM (cont.) — **Encoder selection confirmed:** ALPS EC11E18244AU (replaces prior premium "Grayhill 62 / Bourns optical" placeholder recommendation). Value-tier mid-range encoder at ~$2-3.50 vs $20-40 premium tier; sufficient quality for the use case. Firmware note recorded: 18 PPR / 36 detents is half-step quadrature pattern — decoder must count edges rather than full cycles; software acceleration on long scrolls advised given fine 10° click pitch.
- 2026-05-10 PM (cont., 2nd update) — **ADC selection confirmed for genlock autosense front-end:** Analog Devices AD9204-20 (dual-channel 10-bit 20 MSPS, 1.8 V). Replaces prior AD9201/AD9215 reference — AD9201 confirmed legacy by ADI (Tony M response on EngineerZone). Dual-channel topology lets one chip handle both BNC autosense inputs; pin-compatible upgrade path to AD9231 (12-bit) / AD9251 (14-bit) / AD9258 (14-bit higher MSPS) / AD9268 (16-bit) provides future resolution headroom without board respin. **First DigiKey procurement order placed 2026-05-10:** TE0720-04-31C33MA (commercial-grade variant for bench), TE0703-07 dev carrier, EVAL-ADV7393EBZ, springloaded heatsink (33337). $628.74 incl. shipping. Second order pending.
- 2026-05-10 PM (cont., 3rd update) — **TE0720 silicon variant resolved.** Bench/prototype = TE0720-04-31C33MA (commercial -1 speed grade); production target = TE0720-04-62I33MA (industrial -2 speed grade). Both share 1 GB DDR3L + 32 MB QSPI + 8 GB eMMC memory config and identical pinout — dev work ports directly to production silicon. Evaluated stripped-down TE0720-04-61C530A variant (256 MB DDR3, no eMMC, ~$190) and ruled out — insufficient DDR for video frame buffers + PetaLinux + Node.js + working memory headroom; no eMMC for Linux rootfs (would force SD-card boot or external eMMC on carrier, neither acceptable). Memory cost is real and the cheap variant doesn't have enough.
- 2026-05-10 PM (cont., 4th update) — **First analog picture validated.** R2R DAC + op-amp output stage producing structurally correct NTSC composite signal: ~63 µs line rate, ~5 µs sync width, ~3 µs back porch, ~53 µs active video. Multi-step grayscale staircase test pattern rendering cleanly with crisp transitions and no visible ringing — DAC + op-amp settling adequate for NTSC pixel rates. **Amplitude calibration pending:** signal currently ~3 V p-p (peak white +2 V above blank, sync tip -1 V below blank); SMPTE target is 1 V p-p at 75 Ω (peak white +714 mV, sync tip -286 mV). Sync/video ratio currently ~1:2 (sync depth : peak-above-blank); SMPTE target ~1:2.5. Amplitude scale = analog gain adjustment (bench-side); sync depth ratio = FPGA DAC code value adjustment (Claude-owned iteration). **Workflow clarified:** Claude authors and iterates FPGA HDL; Justin operates bench, captures scope/CRT results, feeds back observations.
- 2026-05-10 PM (cont., 5th update) — **NTSC vertical blanking interval structure verified.** Scope capture confirmed complete, structurally correct VBI sequence on FPGA test pattern output: 6 pre-VSync equalizing pulses (narrow, ~31 µs spacing at 2× H rate) → 3-line broad vertical sync period with 6 serration teeth → 6 post-VSync equalizing pulses → ~12 blank horizontal lines → active video resumes. Matches NTSC spec exactly — signal should successfully lock vertical on standard CRTs. FPGA timing generator HDL validated end-to-end for monochrome NTSC composite output (horizontal + vertical structure both correct). CRT lock test pending (next bench step). Next FPGA milestone: chroma subcarrier (3.58 MHz color burst encoding + chroma modulation onto luma) for color test patterns.
- 2026-05-10 PM (cont., 6th update) — **Frame rate and DAC linearity validated against HDL parameters.** VSync-to-VSync measured at 41.6 ms = 24.04 Hz; cross-referenced against `hdl/vid_timing.v` which specifies `PIXELS_PER_LINE = 3435` (63.613 µs at 54 MHz pixel clock) × `LINES_PER_FRAME = 655` = 41.667 ms = exactly 24.000 fps. Scope matches HDL within measurement tolerance. Earlier diagnostic of "line count off by 6" from a noisy 41.3 ms reading was bad math; code is correct as written. VBI 6+6+6+12 pulse count also cross-referenced: `vbi_gen.v` specifies 3 pre-eq + 3 vsync + 3 post-eq + 12 blank fill lines, each eq/vsync line carrying 2 pulses at half-line spacing — produces exactly the 6/6/6/12 pulses Justin counted on the scope. **Gray test pattern:** flat gray at 2.2 V renders cleanly across full active line; correct front porch / sync / back porch transitions. **Ramp test pattern:** smooth linear ramp from ~1.1 V (at blanking level — verified at tight 5 µs/div zoom) up to peak end-of-line level, with no quantization steps, kinks, or glitches across the full ramp. Confirms R2R DAC ladder is monotonic and linear across the code range, and pixel timing is rock-solid (constant ramp slope = steady pixel clock). Line period ~63.5 µs, active video duration ~52 µs, sync structure all rendering within visual tolerance of NTSC spec at tight zoom. **HDL files in vault confirmed:** `hdl/vid_timing.v`, `hdl/vbi_gen.v`, `hdl/sample_gen.v`, `hdl/top.v`. Workflow going forward: read HDL design parameters first, predict scope behavior, then verify — not the reverse.
- **Pending bench work (non-blocking for FPGA dev):** (a) CRT lock test once CRTs are available, (b) amplitude calibration via bench-side analog gain reduction to land on SMPTE 1 V p-p, (c) tight scope re-capture of ramp minimum endpoint vs back porch level to confirm intentional alignment at blanking. **Next FPGA development milestone:** chroma subcarrier (3.58 MHz NTSC color burst on back porch + I/Q quadrature modulation of chroma onto luma), to be added as a new module integrated with `sample_gen.v` and gated by VBI/active signals from `vid_timing.v`.
- 2026-05-10 PM (cont., 8th update) — **HDCP strategy banked.** V1 Pro Full and Broadcast Digital will ship with **HDCP 1.4 support** on HDMI input. **Bench dev path:** derived/public HDCP 1.4 device keys used for internal bench bring-up only (master key publicly known since 2010 leak, mathematically valid for the authentication protocol but not DCP-issued). Murideo SIX-G (~$2,500) selected as HDCP-capable bench source for authentication testing. **Production path:** DCP Adopter Agreement (~$5K/year for HDCP 1.x) + Xilinx HDCP 1.4 IP license signed before first production run; DCP-issued production keys provisioned per-unit at factory programming step. HDCP 2.2/2.3 deferred until 4K product (consumer 4K content requires HDCP 2.2+). **Mac source workaround during bench dev:** HDFury Diva-class EDID-copying HDMI splitter (~$300) inserted between Mac and Schindler input lets Mac see a real HDCP-compliant TV downstream while Schindler taps the signal — enables Mac bench testing without HDCP infrastructure on Schindler side. **Architecture note:** HDCP handled inside FPGA via Xilinx IP (saves $5–15K licensing? Xilinx HDCP 1.4 IP comes with Vivado Enterprise or as standalone license; DCP Adopter Agreement is separate and required for shipping). Alternative path of moving HDCP to a dedicated chip (TMDS181 has HDCP 2.2 built in, NXP TDA19988 has HDCP 1.4) considered but not chosen — keeping HDCP in FPGA matches the color pipeline architecture and avoids adding silicon. **DCP-issued facsimile/test keys** to be used after DCP license arrives, replacing derived keys, for compliance pre-testing prior to production cert.
- 2026-05-10 PM (cont., 7th update) — **Output DAC, genlock front-end, and sync-conversion architecture decisions banked.** **ADV7393 confirmed with runtime mode selection:** one chip serves composite/S-Video OR component output (mutually exclusive, I²C-switched); never need both modes simultaneously, so a single ADV7393 covers all V1 analog video outputs. **Architecture A confirmed for genlock front-end:** AD9204-20 (dual ADC) + LTC6912 or AD8369 (PGA, selection deferred) + RP2040 (slow-control / autosense decision / Si5351 config) + Si5351 (programmable clock generator) + FPGA (high-rate signal classification of 20 MSPS ADC stream); XLR balanced LTC + 2× BNC autosensing connector mix confirmed. **ADV7280-A dev board to be ordered** for input-side analog video decoder evaluation. **NEW [PROPOSED] V1.5 expansion:** sync conversion with timecode translation — accept genlock+LTC at one rate, output a different rate's genlock+LTC with phase-locked TC math. Would position Schindler as a broadcast-grade rate-domain bridge with CRT driving as one capability (Evertz 5600 / AJA OG class with a CRT driver attached). V1 to preserve architectural option (clock-domain discipline, Si5351 output port reservation, rear-panel provisioning); build as V1.5 / sibling product after V1 ships. New spec section added covering hardware additions, FPGA additions, effort estimate, and architectural design hooks.
- 2026-05-10 PM (cont., 9th update) — **HDMI subsystem architecture finalized; supersedes 8th update.** Iterated through Xilinx-internal HDCP (path priced out at $5-15K license + $5K/yr DCP), then chip-based symmetric HDCP (ADV7611/ADV7513 ruled out as NRND/EOL; updated to LT8619C + IT66121FN), then questioned whether HDCP transmitter chip was needed at all given Schindler's position as a broadcast studio analysis device. **Final architecture:** Lontium **LT8619C** (HDMI 1.4 RX, parallel RGB/TTL out to FPGA, HDCP 1.4 keys embedded by Lontium under their DCP Adopter Agreement) on input + **direct FPGA HDMI 1.4 TX** via free Xilinx HDMI IP (no HDCP encryption on output) + **TPD12S016** ESD protection on both connectors. **Total HDMI subsystem cost: ~$8/unit at qty 100.** **Legal positioning:** Schindler is a broadcast studio device in the lineage of waveform monitors, vectorscopes, confidence preview tools, and signal-analysis equipment (Tektronix WFM, Leader, AJA HDR Image Analyzer category). HDMI output is intended for monitoring/analysis use: waveform/vectorscope visualization, signal lock and status dashboard, test pattern output, color analysis (histograms, gamut warnings, false color), heavily-degraded confidence preview. Architecture follows the letter and spirit of DMCA §1201 (primary-purpose test: signal analysis and color correction, not circumvention) and DCP Compliance Rules (HDMI output does not transmit Decrypted HDCP Content as a faithful copy; analysis/monitoring outputs are industry-standard for HDCP-capable equipment). End-user assumes responsibility for downstream content compliance, mirroring how every professional waveform monitor, scope tool, and broadcast analysis device on the market operates. **Product documentation will state:** "HDMI output is intended for monitoring and signal analysis. Users are responsible for compliance with content licensing and applicable laws regarding downstream signal use." **Cost savings vs symmetric-HDCP path:** no Xilinx HDCP IP license (~$5-15K saved one-time), no IT66121FN transmitter chip ($3/unit saved), DCP Adopter Agreement deferred (likely not strictly required at this scale and use case, but can be added later if Mini consumer SKU eventually ships). **Bench dev path:** LT8619C works out of the box with its embedded HDCP keys — no derived-keys workflow needed for the chip-based input path. For broader HDMI source testing including Macs, HDFury Diva-class EDID copier ($~300) available as bench-only workaround. **Mini variant (deferred):** when/if it ships, Mini targets consumer HDMI market and would require full HDCP 2.2 chain, DCP Adopter Agreement, and likely Xilinx HDCP 2.x IP or equivalent — separate decision at that time.
- 2026-05-11 — **LT8619C eval board + raw chip ordered.** Lontium LT8619C-EVB dev board and a single raw LT8619C chip on order for HDMI input bench bring-up. **HDMI loop-thru feature explicitly deferred** — no LT86102SXE splitter, no second HDMI output, no buffer chip with tap. V1 ships with single HDMI IN (LT8619C) and single HDMI OUT (direct FPGA TX) only. Loop-thru can be revisited as a V1.x or V2 feature if customer feedback requests it.
- 2026-05-11 — **Connector list and panel layout reorganized.** Final V1 rear panel connector list (single-row tall items plus two-row stackable items, 1RU full-rack 19" target): IEC C14 power (35 mm), 2× XLR LTC IN/OUT (52 mm) on single-row track; USB-C service (8 mm), RJ45 GbE (22 mm), 2× SMA WiFi (24 mm), 2× HDMI IN/OUT (44 mm), 2× BNC SDI IN/OUT (36 mm), 4× BNC composite/component IN (72 mm), 4× BNC composite/component OUT (72 mm), 3× BNC autosense IN/LOOP/OUT (54 mm) on two-row stacked layout. Single-row subtotal 87 mm; two-row halved subtotal 166 mm; grand total 253 mm of 432 mm available, leaving 179 mm of unused panel width for future expansion. **V1.5 sync conversion expansion now absorbed into V1** via the LTC OUT XLR and BNC autosense OUT slots — Schindler becomes a broadcast-grade rate-domain bridge with CRT driving as one capability rather than a CRT-only driver. **SDI simplified to 2× BNC** (IN + processed OUT only, dropped passive loop-through from broadcast tier). **Dropped from V1:** DisplayPort IN, mini-DIN S-Video OUT. **TFT and form factor decision:** 1RU full-rack confirmed with two-row connector layout; front panel TFT constrained to ~2.4" 16:9 by 1RU panel height; status-display UI pattern with web UI for detailed config (Evertz 5600 / AJA OG style). Future Mini variant likely compact desktop with 4.3" TFT. **HDMI subsystem chip selection finalized** per 9th update above: LT8619C RX + direct FPGA TX + TPD12S016 ESD protection × 2.
- 2026-05-11 (10th update) — **Physical design decisions banked: cooling, chassis style, half-rack feasibility analysis. PSU decision pending Justin's call.**
  - **Power budget estimate for V1 Pro Full:** Zynq-7020 TE0720 SOM 4–6 W typical / 7 W peak + ADV7393 + ADV7280 ~1 W + LT8619C + 2× TPD12S016 ~0.5 W + AD9204 + LTC6912 + Si5351 + RP2040 ~1.5 W + SDI chips (GS12281 class) ~2 W + misc analog (op-amps, ADCs, LDOs) ~2 W + front panel TFT + USB + Ethernet PHY ~1 W + PSU conversion losses (15%) ~2.5 W. **Total typical: ~14–16 W. Total peak: ~20–22 W.**
  - **Cooling decision: small low-noise PWM fan, designed to be conditional/silent.** Reference fan: Noctua NF-A4x20 PWM (~14 dB at low RPM, ~$15) or equivalent. Chassis designed for fanless operation at typical 14–16 W load via extruded aluminum top cover acting as primary heatsink (Zynq SOM thermally coupled via gap pad). Fan only spins up when SOM heatsink temp crosses programmable threshold under peak load. Broadcast facility rack environments are HVAC-noisy so fan inaudible at rack distance even at full RPM. Pattern proven in modern 1RU broadcast gear (Tektronix, Cobalt, AJA).
  - **PSU decision: PENDING.** Justin's expressed preference: external brick. Claude's recommendation: internal PSU for Pro/Broadcast Digital tier, with rationale: (a) pro market expectation at $2,500+ price point — Tektronix, AJA, Ross, Evertz, Cobalt, Imagine, Lawo all ship internal PSU in 1RU products; external brick reads as prosumer category and caps retail pricing; (b) IEC C14 is a secure locking inlet vs. vulnerable DC barrel; (c) rack-mount cable management cleaner with single AC cord. Compromise option proposed: **internal PSU with redundant DC input** (IEC C14 primary + locking 12V or 24V DC jack as field/redundant power, ~$5 added BOM, OR-ing diodes for source selection). Cisco and Ross both do this pattern. **Trade-offs documented for Justin's final call.**
  - **Chassis style decision: dedicated 1RU full-rack 19" chassis for V1 Pro and Broadcast Digital.** Switch-style box + add-on rack-mount kit pattern (common in network gear: Cisco SG350, Ubiquiti EdgeSwitch, Netgear ProSafe; also some AV: BMD Mini Converters, AJA Hi5) considered and rejected for V1 broadcast tier — rack ears read unprofessional in high-end pro install, chassis depth/width compromises don't serve the I/O density we've committed to, and dedicated 1RU is the universal expectation in fixed-install broadcast (Tektronix, Evertz, Ross openGear, AJA OG-X, Sony BVM, Cobalt). **Switch-style box + kit pattern reserved for future Schindler Mini SKU** where desktop deployment matters and pair-mount in 1RU via joiner kit is a deliberate feature (BMD/AJA pattern).
  - **D-Sub breakout cable feasibility analysis for analog I/O:** Replacing 4× BNC composite IN (72 mm) + 4× BNC composite OUT (72 mm) with one DB-25 (70 mm) or DB-44 HD (70 mm) saves only ~29 mm of panel width given current two-row stacked BNC layout — savings would be larger in a single-row layout but the two-row arrangement already compresses heavily. Combining D-Sub for composite I/O + autosense BNCs into one DB-37 or DB-44 HD saves ~62 mm. **D-Sub approach NOT adopted for V1** — saves modest panel space at the cost of breakout cable management hassle (lost cables, mis-pinning, crosstalk between analog lines requires careful ground-pin placement), and pro broadcast preference is individual labeled BNCs over breakout cables. **D-Sub approach RESERVED for future Schindler Mini-Rack SKU** (half-rack sibling product for mobile production trucks and packed live event racks where panel width is the binding constraint).
  - **Half-rack form factor feasibility analysis:** Half-rack panel width budget is ~215 mm vs. current full-rack layout consuming 253 mm. **I/O panel width is the only binding constraint** — internal chassis volume and thermal envelope are NOT limits at V1's 14–22 W power level. To compress 38 mm into half-rack: (a) external PSU eliminates IEC C14 saving ~27–30 mm but goes against PSU recommendation, (b) D-Sub for composite I/O alone saves ~29 mm (tight), (c) D-Sub for composite + autosense saves ~50–55 mm (comfortable), (d) internal WiFi antennas eliminate 2× SMA saving 24 mm, (e) TRS or sub-mini XLR replacing full XLR LTC saves ~32 mm but loses pro audio expectation. **Half-rack achievable but requires compromises that degrade pro broadcast positioning.** V1 stays full-rack. Design hooks preserved for future Mini-Rack SKU using D-Sub breakout + external PSU + internal antennas combination.
  - **Future product line design hooks banked** (not V1 features): **Schindler Mini-Rack** = half-rack chassis, DB-44 HD analog breakout, external PSU, internal WiFi antennas, targets mobile production trucks and packed live event racks; **Schindler Mini** = switch-style box with optional rack-mount kit accessory, external brick PSU, half-rack-by-pairs in 1RU, targets desktop confidence monitor / prosumer / consumer HDMI calibration market (requires HDCP 2.2 chain and DCP Adopter Agreement — separate decision when that SKU activates).
- 2026-05-11 — **Spec restructure + architectural confirmations.** Revision history split out of `01-spec.md` into this changelog file so the spec doc holds current state only. **Confirmed:** Pi CM4 dropped → Zynq PS hosts control plane under PetaLinux; RP2040 confirmed for genlock slow-control; LTC6912 confirmed as PGA (MIKROE-2555 inbound for bench eval); SDI broadcast tier delivered as **factory-populated hardware option** (not firmware-unlock) with daughter-card-on-headers as field-upgrade vector; XLR LTC IN/OUT **dropped from V1** (panel simplification — LTC routes through autosense BNC input or OUT format selection; may return in a later rev); sync zone restructured to 4 BNCs total (REF IN + REF LOOP on input side, SYNC OUT 1 + SYNC OUT 2 on output side); each SYNC OUT runtime-selectable across black burst / tri-level / LTC; hardware driver chain spec'd to also support **DARS + Word Clock** via firmware-only upgrade later (driver bandwidth DC to ~10 MHz, swing ≥2 Vpp into 75 Ω). Per-OUT generation via independent FPGA phase accumulators off the master clock — Si5351 ch1/ch2 stay reserved (future GPSDO 10 MHz distribution candidate). New `docs/signal-flow.md` with Mermaid block diagrams (video signal path, sync subsystem, control plane). New `docs/panel-layout.md` with rear-panel ASCII sketch.
- 2026-05-11 — **Rear-panel status LCD + per-connector status LEDs confirmed.** 2.4" 16:9 SPI LCD (ILI9341 / ST7789 class) on rear panel, Zynq PS owned, ~1 s refresh, read-only status grid (one row per I/O connector with name + status icon + detail; header bar shows IP/hostname/firmware/current reference). Tricolor R/A/G LED at every rear-panel connector, driven by 3× TLC59116F constant-current I²C drivers (48 channels total), Zynq PS owned. Convention: input red = expected/missing, amber = present/not-in-use, green = present-and-in-use; output green = configured + outputting, amber = configured but no source; sync IN red = invalid, amber = locked-but-not-selected, green = locked + selected. Default brightness ~10 % so the panel isn't a Christmas tree in a dark machine room. Front-panel LED column mirrors rear state for at-rack-or-front visibility parity. Zynq PS holds the I/O state aggregator (SSOT) and pushes mirror updates to UI MCU.
- 2026-05-11 — **Power & safety architecture confirmed.** Pre-certified PSU module path (custom AC-DC design intentionally avoided — ~$15–25K + 3–6 months NRTL certification saved at Schindler's ~75 unit lifetime volume). IEC inlet: Schaffner **FN9260B-6-06** (C14 + 6 A + integrated fuse holder + 1-stage EMI filter, panel-mount, ~$18). Mains fuse: **2 A T (time-lag), 5×20 mm** in FN9260B holder — sized for 120 VAC worst-case (steady state ~0.21 A RMS gives 7–10× headroom, comfortably absorbs ~25 A / 5 ms cold-start inrush). No rocker switch (IEC cord = service disconnect, front soft button for daily use). PSU module: **TDK-Lambda HWS50A-12/A** primary (50 W / 12 V / UL 62368-1 listed / DigiKey-stocked, ~$50); **Mean Well LRS-50-12** drop-in alternate (~$15–20, higher availability). Mains wiring inside chassis: UL 1015, 18–20 AWG, three-conductor. Single-point earth via chassis ground stud (IEC + PSU + chassis bonded; carrier digital ground tied at one point to prevent ground loops). **Carrier 12 V input protection chain:** Diodes Inc **DMP3098L-7** P-FET reverse-polarity (−30 V, R<sub>DS(on)</sub> 31 mΩ, gate to GND via 100 kΩ) → Bourns **MF-MSMF200-2** polyfuse (I<sub>hold</sub> 2 A / I<sub>trip</sub> 4 A) → Littelfuse **SMBJ12A** TVS (unidirectional, V<sub>C</sub> 19.9 V peak, 600 W) → TI **INA226** + 5 mΩ shunt (I²C power monitor; telemeters 12 V rail voltage + actual current draw to Zynq PS for rear LCD / web UI; doubles as PSU-healthy check since HWS50A has no DC-OK signal pin) → 3× Murata **GRM32 22 µF 25 V X7R** MLCC bulk (~66 µF, chosen over electrolytics for 10+ year service life). Locking **Molex Mini-Fit Jr.** 2-pin connector from PSU to carrier. Total carrier protection BOM ~$6.20. Per-rail bucks/LDOs (5 V, 3.3 V, 1.8 V, 1.35 V, 1.0 V, VAUX) selected at carrier-schematic time — each has built-in OCP + thermal shutdown so no extra protection layer needed.
- 2026-05-11 (PM) — **DigiKey procurement order placed (Web ID 371561295, $353.39).** Executing on prior banked decisions plus committing two new analog op-amp selections for the output buffer stage.
  - **NEW DECISIONS banked from this order:**
    - **Output analog buffer architecture: two-tier op-amp strategy.** **OPA2350UA/2K5** ×5 (TI CMOS dual op-amp, 38 MHz GBW, rail-to-rail, 8-SOIC, ~$5) for SDTV composite / S-Video buffer paths after ADV7393 DAC. **LMH6643MAX/NOPB** ×10 (TI voltage-feedback op-amp, 130 MHz BW, low distortion, 8-SOIC, ~$1.50) for higher-bandwidth component and SDI-adjacent driver stages. Rationale: OPA2350 is rail-to-rail with adequate bandwidth and clean SDTV performance at a lower cost; LMH6643 reserved for component paths where bandwidth and distortion matter more. Both chips are TI-pedigreed, available, and widely used in pro video output stages. Final per-channel assignment deferred to carrier schematic phase.
    - **MCP6022-I/P** ×5 also included — 8-DIP general-purpose dual op-amp for non-critical breadboarding and sanity-check circuits. **Not the V1 production selection** — prototyping stock only.
  - **EXECUTING ON banked decisions (specific variants now committed):**
    - **ADV7393BCPZ-REEL** ×1 (~$16) — output video DAC/encoder banked in 7th update. **BCPZ** variant = industrial grade (no automotive AEC-Q qualification), 40-LFCSP package, reel packaging. Single chip serves composite/S-Video OR component output via I²C runtime mode selection.
    - **ADV7280AWBCPZ-M-RL** ×1 (~$19) — input video decoder per today's recommendation. **AW** variant = automotive AEC-Q100 qualified + extended −40°C to +105°C temp range (vs +85°C non-W). **-M** variant adds MIPI CSI-2 output alongside parallel YCbCr (parallel used for V1; MIPI as future-proofing for possible Mini variants).
    - **LTC6912CGN-2#PBF** ×1 (~$8) — PGA for genlock front-end, already confirmed in spec restructure entry. Resolves earlier "LTC6912 or AD8369 deferred" item from 7th update; AD8369 ruled out.
    - **EC11E18244AU** ×5 (~$4/unit) — Alps EC11 production encoder, banked in earlier spec entries.
    - **Si5351 eval boards (Adafruit 2045)** ×2 for genlock PLL prototype + parallel timing reference path development.
    - **Mean Well LRS-50-12** ×1 ($12) — internal PSU per power architecture entry (drop-in alternate to TDK-Lambda HWS50A; lower cost / higher availability path for bench prototype).
    - **Schaffner FN9260B-6-06** ×2 ($18 each) — IEC C14 filtered inlet per power architecture entry. Two ordered for V1 prototype + spare.
  - **FRONT PANEL UX EXPERIMENTATION:**
    - **3315Y-025-016L** ×2 alternate encoder for comparison feel vs. EC11.
    - Five knob style options for hand/eye testing: **CP34501** (C18 black, 6 mm spline), **FC7229NML** (KMR12B black, 6 mm spline), **CL178883** (K21 black, 6 mm D-shaft), **FC1611** (K1C black, 6 mm SF MD), **1202CY** (0.236" phenolic). Plus **CL1730** caps. **Production knob selection deferred to physical evaluation.**
    - **SPK-023A** ×5 black plastic spacers (mounting hardware).
  - **PROTOTYPE CONNECTORS & PASSIVES:**
    - 75 Ω PCB-mount BNCs: **0731711900** straight ×5 + **0731010401** right-angle ×5 for prototype carrier boards.
    - **CP-88T-75** ×4 BNC 75 Ω terminators for impedance testing and unused-output termination.
    - Resistor prototyping stock: **MFR-25FTE52-3K3** (3.3 kΩ, 1%, 1/4 W) ×30 + **MFR-25FRF52-1R** (1 Ω, 1%, 1/4 W) ×25 + **MFR-25FTE52-2R** (2 Ω, 1%, 1/4 W) ×25.
  - **STILL PENDING procurement (not in this order):** AD9204-20 dual ADC (other half of genlock front-end paired with LTC6912 ordered here), LT8619C raw chip + LT8619C-EVB (separate order via L2tek / AvaQ / Lontium direct, already on order separately per earlier today's entry), TPD12S016 ×2 HDMI ESD protection, MIKROE-2555 LTC6912 GainAMP click board (inbound per spec restructure entry), Murideo SIX-G HDCP test source (~$2,500 when budget allows), Trenz TE0720 SOM commercial-grade bench unit + TE0720-04-62I33MA production unit.
- 2026-05-11 (PM, follow-on) — **Second procurement order placed: AD9204 ADC + GainAMP click + additional encoder. Closes the genlock front-end bench parts list.**
  - **AD9204 procurement complete:** **AD9204BCPZ-20** ×1 (~$16) — production chip, dual 10-bit ADC, 20 MSPS speed grade, banked in spec restructure entry for genlock front-end. **AD9204-80EBZ** ×1 (~$278) — AD9204 eval board for bench characterization. Note: eval board uses the 80 MSPS speed grade rather than the 20 MSPS production grade; this is a deliberate bench choice (the 80 MSPS board can be downclocked to 20 MSPS for production-equivalent testing while also enabling higher-rate bench characterization for signal diagnostics and sync detail capture). Production carrier uses the cheaper AD9204BCPZ-20.
  - **GainAMP click confirmed on order:** **MIKROE-2555** ×2 (~$25 each, ~$50 total) — LTC6912 GainAMP click board for bench characterization of the genlock PGA. Noted as "inbound" in spec restructure entry; now confirmed quantity 2 ordered. Two enables parallel bench setups: one paired with AD9204 eval for full front-end chain bench, one standalone for PGA-only bandwidth characterization at max gain.
  - **Additional encoder for UX testing:** **EC111012010H** ×1 (~$10) — Alps Alpine 5V through-hole encoder variant, different physical configuration from the EC11E18244AU ×5 production encoder ordered earlier today. Continues the front-panel UX experimentation pattern. Final production encoder selection still EC11E18244AU per spec; EC111012010H is comparison-feel testing only.
  - **GENLOCK FRONT-END BENCH PARTS LIST NOW COMPLETE:** AD9204BCPZ-20 (ADC, production) + AD9204-80EBZ (ADC eval) + LTC6912CGN-2#PBF (PGA, production, from earlier today's order) + MIKROE-2555 ×2 (PGA eval) + Si5351 Adafruit 2045 ×2 (clock gen eval, from earlier today's order). All four functional blocks of Architecture A genlock front-end (ADC + PGA + clock gen + FPGA classification) have bench-evaluable hardware on order. RP2040 slow-control board status: assumed on-hand from prior projects or already in vault; verify.
  - **REMAINING procurement targets (post this order):** LT8619C-EVB + raw chip (separate order via L2tek/AvaQ already in flight), TPD12S016 ×2 HDMI ESD protection, Trenz TE0720 SOM (commercial bench + industrial production grades), Murideo SIX-G HDCP test source (~$2,500, budget-gated). With those, the V1 hardware parts list is essentially complete pending carrier PCB schematic phase.
- 2026-05-11 (PM, third follow-on) — **TPD12S016 procurement added: eval board + bare chip.** **TPD12S016PWREVM** (TI HDMI port protection eval module, gives hands-on with the chip's TVS clamps, DDC/HPD level shifters, and 5V cable-power switch before carrier PCB layout) + bare **TPD12S016** chip for the eventual carrier. Matches the established eval+production procurement pattern used for ADV7393, ADV7280, LT8619C, AD9204, and LTC6912. Closes the HDMI ESD protection line in the V1 hardware parts list. V1 carrier needs 2× TPD12S016 (one per HDMI port — input paired with LT8619C, output paired with direct FPGA TX); bare-chip quantity should be ≥2 for prototype build plus a spare for rework.
- 2026-05-11 (PM, scope clarification) — **HD pipeline + per-output independence + HDCP UI consent gate confirmed.** Pipeline scope broadened from "drive composite to a CRT" to "process arbitrary HD video signal, output to multiple formats and rates concurrently." Key shifts:
  - **FPGA pipeline is HD-bandwidth throughout** (RGB or YCbCr 4:2:2, up to 1080p60 / 148.5 MHz). Source rate preserved through scaler / color / geometry. Downconversion and rate-conversion happen **only inside per-output terminal encoders** — not as a single monolithic step.
  - **Outputs are independent and concurrent.** Same source video can drive 1080p60 HDMI + NTSC composite (with cadence convert) + HD component (YPbPr) + SDI passthrough — all live, all at their own format and rate.
  - **HDMI OUT repositioned from "monitoring/analysis only" to "full-quality HD passthrough."** Earlier (9th update) framing of HDMI OUT as a heavily-degraded analysis output is obsolete. HDMI OUT now carries the full pipeline at source rate.
  - **HDCP architecture: default-block + UI consent override.** Default behavior blocks HDCP-protected source content from full-quality HDMI OUT (protected content can still flow to analog outputs which don't carry HDCP, and to SDI OUT as a broadcast-workflow assumption). Operator unlocks HDMI passthrough of protected content via a UI dialog requiring explicit attestation ("I attest this is a non-violating use"). Non-protected content flows through HDMI OUT with no gate.
  - **Legal positioning (attorney-advised):** keeps the device's default behavior compliant with DMCA §1201 / DCP Compliance Rules, and shifts compliance responsibility to the operator for any override. Pattern mirrors how Decimator / some BMD products handle HDMI passthrough on HDCP-capable equipment. Still avoids the Xilinx HDCP IP license + DCP Adopter Agreement cost path explicitly avoided in the 9th update.
  - **Frame rate matrix expanded:** pipeline throughput rates now include 1080p50 / 1080p59.94 / 1080p60 / 720p50 / 720p59.94 / 720p60 alongside the CRT-driving rates (23.976 / 24 / 25 / 29.97 / 30). Per-output terminal encoders target their own rates with cadence-convert logic for rate translation when input ≠ output (60 → 24 via 5:2 pulldown + crossfade, 60 → 25 via 6:5, etc.).
  - **HDL implications:** the `vid_timing.v` / `vbi_gen.v` / `chroma_gen.v` / `sample_gen.v` already written for Phase 2 first-light constitute the composite encoder terminal, not the whole pipeline. The HD scaler / color / geometry stages and other terminal encoders (HDMI passthrough, component, SDI passthrough) are downstream HDL work.
  - **`signal-flow.md` diagram 1 rebuilt** to reflect: source mux → VDMA → HD pipeline → HD signal bus → fans out to independent terminal encoders → physical outputs.
- 2026-05-11 (PM, wisdom banked) — **Wide-back-porch default for 24p camera shoots.** Per industry wisdom from a 24-frame production veteran (relayed by Justin): when a CRT is being filmed at 24p, a broader-than-nominal back porch gives the camera shutter a larger target window to land its capture inside the active video region without straddling the V-blank — which is what produces the visible "sync bar" stripe on filmed CRT footage. Recommended default for "24p camera shoot" CRT profiles: back porch ≈ 7–10 µs (1.5–2× SMPTE 170M nominal ~4.7 µs). Tunable per-profile so DPs can adjust to their shutter angle. This is a default-tuning note for the per-profile sync structure parameters already banked in the CRT-specific signal controls section — recorded here to make sure the wisdom isn't lost between profile authoring sessions.
- 2026-05-11 (PM, sweep) — **Spec doc cleanup pass.** Removed or updated 15 stale items found in a systematic re-read of `01-spec.md`: dropped DisplayPort IN references (it was killed in the 2026-05-11 connector reorg); replaced "TI TMDS141 retimer" with the actual LT8619C HDMI RX chip in two places (Hardware foundation + V2 reference); reframed S-Video output as silicon-capable / no V1 connector (rather than a V1 output bullet); removed the duplicated Front panel section that appeared twice after the rear-LCD insert; rewrote the V1.5 sync-conversion section as a banked-feature description ("Sync conversion capability — V1 absorbed") since dual SYNC OUT + per-format selection covers it; updated the V2 / future section to reflect HD-pipeline scope (CRT-driving is one use case, not the only one); rewrote the HDR out-of-scope rationale (the old "Output is composite to a CRT" reasoning was wrong post HD-pipeline expansion); and resolved/removed two open questions that were already banked (rear-panel layout — done in panel-layout.md; PSU style — banked in Power & Safety section). **PSU primary/alternate flipped:** Mean Well LRS-50-12 is now primary (cheaper, wider availability, on order), TDK-Lambda HWS50A-12/A is alternate (lower noise, swap-in if Mean Well noise shows up as audible during bench characterization). Updated `bom-v1.md` to match the flip.
- 2026-05-11 (PM, RF tier) — **RF modulator daughter card banked as [PROPOSED] V1 Period tier.** Third SKU alongside Base and Broadcast, adds RF output on a TV channel carrier (NTSC Ch3/Ch4 + extensible to VHF E2–E12 and PAL-M) for the 1970s-and-earlier consumer CRT market segment whose sets have only an antenna/RF input. Mirrors the SDI daughter card mezzanine pattern: footprints + headers on every V1 carrier; populated at factory for Period SKU, blanked on Base/Broadcast, field-installable upgrade path via dealer. Stackable with SDI daughter card so a single unit can carry both for full-kitchen positioning.
  - **Course correction from AM analysis:** Initial 2026-05-11 AM session floated AD725 as a "simple integrated modulator." Re-read of the AD725 datasheet confirms it is an RGB-to-composite encoder with a luma-trap port designed to feed an external RF modulator — it does NOT itself modulate to RF. Discarded. Single-chip composite-to-RF modulator ICs (LM1889, MC1374, CXA1645, TA8717 class) are all discontinued; consumer RCA-to-RF boxes use unsourceable no-name ICs. Modern path is discrete VSB-AM modulator from in-production parts.
  - **Architecture:** carrier provides buffered composite tap (one additional LMH6643 op-amp channel from existing chip) + I²C + power + ENABLE GPIO over a small mezzanine connector. Daughter card hosts video buffer, local Si5351 for RF carrier, analog multiplier IC for AM modulation, output LC LPF for harmonic suppression, MMIC RF amp, 50→75 Ω match, panel-mount F-connector, shield can over RF section.
  - **Modulation:** DSB-AM (no VSB filter, simpler than VSB at the cost of 2× spectrum; consumer VSB receivers filter the unwanted sideband internally anyway). No audio carrier in V1 — Schindler has no audio pipeline; most consumer tuners display picture fine without 4.5 MHz audio subcarrier (speaker stays silent rather than buzzing). Audio pilot carrier deferred to V1.x if customer reports drive it.
  - **Tap point trade-off (banked):** post-ADV7393 buffered composite. Implies RF output rides on whatever format/rate the composite encoder is configured for (operator selects NTSC-M on composite OUT → RF also NTSC-M; switch to component mode → composite + RF both go dark). Documented limitation; future revision could add dedicated composite encoder on daughter card to decouple but not justified pre-evidence.
  - **Channel coverage:** VHF Ch2–Ch13 (54–216 MHz), software-selectable via I²C from UI. Covers US NTSC-M Ch3/Ch4 + optional Ch2/5/6, Japan NTSC-J Ch1/Ch2, PAL-B/G Europe E2–E12, PAL-M Brazil. UHF (>300 MHz) deferred.
  - **Output level:** ~−40 to −30 dBm at F-connector, adjustable, short-coax-into-CRT design (not broadcast). Stays inside FCC Part 15.119 cable-output limits with shielded RF section + output LPF (2nd harmonic ≥30 dB below fundamental).
  - **Parts banked (with bench-eval-phase final selection on the modulator chip):**
    - **AM modulator:** **ADL5391ACPZ** primary (Analog Devices DC–2.0 GHz multiplier, 16-LFCSP, ~$18 at qty 100, modern symmetric-core architecture); **AD835ARZ** fallback (250 MHz multiplier, 8-SOIC, ~$25 at qty 100, classic part with abundant reference designs). Order both for prototype bench eval.
    - **RF carrier gen:** **Si5351A-B-GT** local on daughter card (~$1.50) + 25 MHz crystal (~$0.50). Decouples from carrier's genlock Si5351; carrier's ch1/ch2 stay reserved for future GPSDO 10 MHz distribution.
    - **RF amp:** Mini-Circuits **ERA-3SM+** MMIC (DC–3 GHz, ~22 dB gain, ~$3.50, SOT-89, 50 Ω native — needs simple 50→75 Ω match).
    - **Output filter:** 5th-order Chebyshev or Butterworth LC LPF (~$1.50 in passives — Coilcraft 0805CS class inductors + C0G ceramic caps).
    - **F-connector:** Amphenol RF 82-4421 class panel-mount 75 Ω (~$1.50).
    - **Shield can:** Wurth WE-SHC or Laird small-format with frame (~$2.50).
    - **ESD on F-conn:** PESD3V3L1BA class low-cap TVS (~$0.20).
    - **DC block:** C0G 0.1 µF 50 V (~$0.10).
    - **Mezzanine:** Samtec **TSM-105-01-T-DV** (header) + **SSM-105-S-DV** (socket), 10-pin 0.1" dual-row, ~$3 the pair. Single connector pair sufficient since no high-speed digital crosses the boundary (composite is 6 MHz baseband; I²C and power are slow).
    - **Mezzanine pin assignment:** GND × 4, +3.3 V, +5 V, COMPOSITE (carrier → card, 75 Ω, 1 Vpp), I²C SCL, I²C SDA, ENABLE (Zynq PS GPIO active-high).
    - **Daughter card PCB:** 50×50 mm 2-layer FR-4 with solid ground plane under RF section (~$4 at qty 100 via JLCPCB / PCBWay).
  - **Carrier-side additions (on every V1 carrier, base + broadcast + period):** mezzanine header footprints + termination passives + one additional LMH6643 op-amp channel routed to header for composite tap leg. ~$1.50 in unpopulated passives on Base/Broadcast carriers (header lands unpopulated). No silicon impact on Base/Broadcast.
  - **Rear-panel mechanical:** single F-connector cutout (~10 mm), blanked on Base + Broadcast SKUs, populated on Period SKU.
  - **Daughter card BOM total: ~$38 per unit at qty 100.** Period SKU adds this on top of Base.
  - **FCC strategy:** stay within Part 15.119 cable-output device limits (shielded RF section + LPF harmonic suppression + low output power + coax-only output). Expected formal Part 15 test campaign: ~$5–10K at an accredited lab when SKU ships commercially. Adds to Schindler cert scope but isolated to Period tier.
  - **Open questions documented for bench-eval phase:** (a) final modulator chip ADL5391 vs AD835; (b) output LPF topology (Chebyshev vs Butterworth); (c) 50→75 Ω matching (resistive pad vs Mini-Circuits TC4-1W+ transformer); (d) Si5351 reference source (local 25 MHz crystal vs carrier reference); (e) V1.x audio carrier pilot decision pending customer evidence; (f) channel-selection UI surface (front-panel vs web-only) pending UI spec phase.
  - **Full architecture and parts spec banked in new doc:** `docs/rf-modulator-daughter-card.md`. Spec entry added to `01-spec.md` between SDI daughter card section and Sync conversion capability section. BOM section 7 added to `bom-v1.md`.
- 2026-05-11 (PM, RF locked in) — **RF subsystem confirmed: baked into V1 carrier, not a daughter card. SKU axis collapses to Base + Broadcast.** Five decisions banked this session:
  - **EVALZ pricing correction.** ADI ADL5391-EVALZ is ~$300 at DigiKey, not the $95 I'd quoted earlier. Confirmed cheap-bench-eval path: **EC Buying ADL5391 breakout board** (~$15-25 AliExpress) + **ADL5391ACPZ-R7** bare chip ($15 DigiKey, known-authentic backup against counterfeit-chip risk on Chinese board) + **AD835ARZ** ($25 DigiKey, architectural fallback). Total ~$55 in silicon vs $300 ADI EVALZ. Justin's pattern (cheap thing for bench + known-good backup) applied.
  - **"One analog out, selectable" architecture committed.** Operator picks one of three analog output modes via UI: composite (BNC live), RF Ch3 or RF Ch4 (F-conn live, ADV7393 stays in composite mode), or component (3× component BNC live, ADV7393 in component mode). Composite and RF share the same ADV7393 composite encoding; they differ only on which output path is gated. HDMI and SDI remain independently live regardless of analog-out mode. Mode-mux on the carrier: ADG419 SPST switch on composite BNC + FET switch on ERA-3 bias supply, both driven by Zynq PS GPIOs.
  - **Bake into every carrier, drop daughter-card framing.** BOM delta (~$33) too small to justify SKU bifurcation. Customer segments fuzzy (any DP could encounter a period CRT). FCC scope already covers every unit (WiFi/BT cert mandatory regardless), so RF is incremental in cert, not bifurcating. Clean product story: every Schindler 2.0 has HDMI + SDI + one selectable analog out. **SKU axis collapses to Base + Broadcast only.** Period SKU tier eliminated.
  - **Ch3/Ch4 only — NTSC US scope.** Drops earlier VHF Ch2-13 framing. US-centric customer base, every period US set tunes Ch3 or Ch4 as VCR-connect channel pair. Cleaner spectrum: 56–73 MHz bandpass filter covers both video + audio carriers for both channels, rejects everything outside band including 2nd harmonics (122.5–134.5 MHz). Eliminates need for separate harmonic LPF. Simpler UI: Ch3/Ch4 toggle. PAL/NTSC-J/PAL-M expansion deferred to firmware-only V1.x if international demand materializes (same hardware, different Si5351 register loads + wider bandpass swap).
  - **Audio Path A confirmed (silent pilot, no FM modulation).** Si5351 ch2 generates unmodulated CW carrier at video_carrier + 4.5 MHz (65.75 MHz for Ch3, 71.75 MHz for Ch4). TV's audio detector sees carrier presence (satisfies intercarrier AGC on touchy 1970s sets); no audio to demodulate; speaker stays silent. Schindler's mission is camera-captured visuals; film cameras don't record CRT speaker audio, so audible audio is not a requirement. **Audio FM modulation analysis documented but deferred:** if V1.x customer evidence drives speaker output need, the Path B approach (discrete Colpitts VCO with varactor, FPGA-generated 1 kHz test tone via PWM + RC LPF, sigma-delta modulator in fabric — ~$3 BOM, ~1 day HDL) is the documented next step. 2-channel "stereo" via BTSC MTS explicitly rejected for V1 (BTSC didn't exist until 1984; every period set in target market is mono).
  - **Audio FPGA capability noted for future reference:** Zynq-7020 can generate audio trivially (NCO + sigma-delta + PWM out + audio buffer). Audio I/O is the gating question, not FPGA capability. Si5351 cannot FM-modulate at audio rates (I²C register-write rate is way too slow), so FM audio requires either a discrete VCO (Path B) or FPGA-direct synthesis via a fast DAC (Path C — over-engineered for V1).
  - **RF subsystem BOM (per V1 unit, all SKUs):** ADL5391ACPZ-R7 ($18) + dedicated Si5351A-B-GT + crystal ($2) + ERA-3SM+ ($3.50) + bandpass LC filter ($1.50) + 50→75 Ω match ($1) + audio combiner passives ($0.50) + mode-mux ADG419 + FET ($1.80) + F-conn + ESD + DC block ($1.80) + shield can ($2.50) + bypass passives ($1) = **~$33 per unit**. Built into every V1 carrier; no daughter card, no mezzanine, no separate PCB.
  - **Dedicated Si5351 for RF subsystem** (separate from genlock Si5351). Reasoning: genlock Si5351 is constantly nudged by FPGA loop filter to track reference; RF wants stable fixed frequencies. Cross-coupling avoided by separation. Genlock Si5351 ch1/ch2 stay reserved for future GPSDO 10 MHz distribution. RF Si5351 channel allocation: ch1 = video carrier (Ch3 61.25 MHz / Ch4 67.25 MHz), ch2 = audio pilot (Ch3 65.75 MHz / Ch4 71.75 MHz), ch0 free.
  - **Rear-panel placement:** F-connector lives **beside** the composite BNC. Both physically present, only one electrically live at a time per mode-mux state. Absorbed in the existing ~179 mm of unused panel slack documented in `panel-layout.md` — no connector reorganization needed.
  - **Updated BOM rollup:** Base unit ~$817 (was $784, +$33 RF). Broadcast unit ~$849 (was $816, +$33 RF). Period tier eliminated.
  - **Doc operations:** New authoritative doc `docs/rf-modulator-subsystem.md` written. Prior doc `docs/rf-modulator-daughter-card.md` overwritten with redirect stub (can be deleted at convenience). `01-spec.md` RF section rewritten from `[PROPOSED]` daughter card to confirmed built-in. `bom-v1.md` Section 7 rewritten; rollup table loses Period column.
- 2026-05-11 (PM, MVPHD bank) — **10 high-priority MVPHD-feature gaps closed for V1.** After authoring `mvphd-comparison.md` and reviewing the gap list, Justin confirmed 9 items as must-have and one (#2 EFX) expanded to 13 effects total. Banked into `01-spec.md` + `ui-menu.md` + `mvphd-comparison.md`.
  - **Still image buffers** (4 FLASH-backed slots on TE0720 eMMC, selectable as input source). Use cases: power-on splash, idle reference, brand idents, signal-loss fallback to a known image. Storage budget ~24 MB total of 8 GB eMMC.
  - **Front-panel BLACK quick-action** — fade-to-black with configurable transition time, default Q1 binding.
  - **Front-panel MONO quick-action** — monochrome toggle, default Q2 binding.
  - **Shutter Phase Reference test pattern** (renamed from MVPHD's F1-BLU/F2-YEL — the underlying alternating-field two-color signal is industry convention, not proprietary). Default Blue/Yellow; color pair operator-configurable (also Red/Cyan, Magenta/Cyan, Black/White, Custom). Visual companion to the wide-back-porch wisdom for filming CRTs.
  - **YUV color-circle color-temperature adjust** — X/Y axes on the chroma plane, in addition to RGB white-point trim. Preserves luminance + saturation when shifting color temp. Inherited from MVPHD Ch. 5 approach; preferred by video colorists.
  - **3:2 cadence auto-detect** + **Motion filter selection** (Quadratic / Linear / Off). Cleaner 24 p extraction from telecined 30 / 60 fps source than blind frame-rate conversion.
  - **Proc Amp Enable bypass** — single per-input toggle to disable all proc-amp adjustments. Default Q3 binding for raw-vs-adjusted quick compare.
  - **Custom user-loaded test signals** (8 slots, web upload, eMMC-persistent). Operator can add focus charts, alignment grids, brand idents.
  - **14:9 Letter/Pillar Box ARC mode** + reworked § 4.2 Aspect Ratio Conversion showing both directions (16:9 → 4:3 AND 4:3 → 16:9) per the MVPHD pattern operators are familiar with.
  - **EFX library (full 13 effects + modifiers)** — Justin chose to extend beyond MVPHD's 5 with on-brand CRT-prop effects. The list:
    - From MVPHD: Freeze frame, Random noise, Block artifacts (collapsed from MVPHD's small/large split into one intensity-configurable effect), Fade-to-Black (handled by the BLACK button).
    - New on-brand CRT/period effects: CRT power-off (collapse to line → dot → flash → black, ~1.5 s — Justin's idea, on-brand differentiator), CRT power-on (paired flash + scan-in), Vertical hold loss (rolling), Snow burst (channel change), Color rolling (chroma desync), VHS tracking error (drifting noise bars), Hum bars, Burn-in ghost overlay, Scanline emphasis (for **CRT look on a downstream LCD** — shoot an LCD prop that reads as a CRT in-camera), Blur (Gaussian — Tier 3 HDL cost, may land later in V1), Solarize, Posterize.
    - Modifiers: blend/mixer (0–100 % wet/dry), transition time (0–240 frames), per-effect intensity parameters.
    - Profile-persistent: effects can be temporarily toggled OR saved into a per-CRT profile.
    - HDL effort: ~2–3 weeks for MVPHD set + Tier 1 CRT effects post-first-light; blur deferred if needed.
  - **Skipped (per Justin's call):** Y/C Delay input control. Analog composite isn't the headline feature; silicon supports it for V1.x firmware add if a niche customer asks.
  - **UI menu doc restructure:** new § 8 Effects inserted; § 8-12 renumbered to § 9-13 (Profiles / Behavior / Test+Maint / System / Compliance). Cross-references updated.
- 2026-05-11 (PM, still buffers detail) — **Still image buffer + microSD slot specifics banked.** Following the MVPHD-bank entry above, detail-pass through the still image buffer feature:
  - **Format:** PNG, up to 1920×1080 (downscaled on the fly to active output rate).
  - **Storage tier 1: TE0720 eMMC** (4 slots, ~25 MB of 8 GB used). Always present, fast.
  - **Storage tier 2: front-panel microSD slot** (resolves prior `panel-layout.md` open question — now confirmed). Push-push panel-mount socket, ~$2 BOM. Dual-purpose: (a) firmware updates without rear-panel access (MVPHD-familiar workflow), (b) extended still-image library — operator loads images from card into one of the 4 active buffers.
  - **Load time:** < 1 s cold (Zynq A9 dual-core PNG decode ~250–500 ms dominates), < 10 ms cached in DDR3 after first load.
  - **First-boot state:** Buffer 1 pre-populated at factory with Schindler splash (logo + IP + firmware version, identifiable before any config). Buffers 2–4 ship empty.
  - **Burn-in ghost source:** EFX § 8.4 burn-in ghost overlay sources its image from one of the 4 still buffers (no separate upload mechanism).
  - **Static only in V1** — no animated buffer support; static-image is sufficient for the planned use cases.
  - **Thumbnails:** front-panel TFT shows 2×2 grid (~220 × 128 px each at production 480 × 272 panel resolution); web UI shows full-quality thumbnails.
  - **Front-panel TFT resolution decision:** production target confirmed as **480 × 272 WQVGA on 2.8" 16:9 LTDC** parallel panel (sweet spot for 1 RU panel height + thumbnail readability). Prototype path stays on 320 × 240 ILI9341 SPI.
  - Updated `01-spec.md` Still image buffers + Front panel sections, `ui-menu.md` § 1.7, and `panel-layout.md` front-panel inventory + ASCII sketch + open-question resolution.
- 2026-05-11 (PM, display part-number bank) — **Front + rear display modules confirmed.** Replaces the prior aspirational "2.8" 16:9 WQVGA" + "2.4" 16:9 SPI" placeholders with actual Newhaven part numbers after dimensional review of three candidate datasheets (Justin's downloads). Decision drivers: (a) 1 RU front-panel vertical budget hard-capped at ~30 mm — rules out standard 2.8" 4:3 modules whose outline is 50 mm tall, (b) bar / ultra-wide TFT form factor is the pro-broadcast 1 RU convention.
  - **Front TFT: Newhaven NHD-2.9-376960AF-ASXP** — 2.9" 376×960 IPS TFT, ST7701SN controller, mounted **rotated 90° to landscape (effective 960 × 376)**. Module outline 32.6 × 78.7 mm; bezel-opening cutout **69 × 28 mm** in landscape orientation; recessed pocket ~80 × 33 × 3 mm. Interface options: 24-bit parallel RGB **OR 4-wire SPI** — the SPI path lets the STM32H735 drive it from a standard SPI peripheral without LTDC parallel routing. 190 PPI density (~6× the pixel count of the prior 480×272 plan) → crisp text, sharp buffer thumbnails. Chosen over the alternative NHD-3.9-480128AF-ASXP bar TFT (3.9" 480×128, 119 PPI) because of the 6× pixel count and the SPI interface option, despite the 3.9" being marginally wider/more "broadcast-bar-look."
  - **Rear status LCD: Newhaven NHD-1.5-240240AF-CSXP** — 1.5" 240×240 IPS square TFT, ST7789VI controller (common, well-supported by Zynq Linux fbtft). Module outline 32.52 × 35.32 mm; active-area cutout **28 × 28 mm**; recessed pocket ~33 × 36 × 3 mm. Interface: 8-bit 8080-II parallel OR 3/4-wire SPI (SPI preferred — one Zynq PS SPI peripheral). The 1:1 square aspect requires a redesign of the rear status view from "full I/O grid" to "paginated summary" (selected ref + lock state + IP + page-through I/O detail) — workable since the rear LCD is read-only and the operator scrolls pages by approaching the rear panel anyway.
  - **Thumbnail layout updated:** still-buffer thumbnails on the front-panel TFT change from a 2×2 grid (at the prior 480×272 plan) to a **4-wide horizontal strip** at the new 960×376 landscape resolution. Each thumbnail ~240 × 180 px with buffer name + slot number label below. Web UI still shows full-quality thumbnails.
  - **BOM impact:** front TFT ~$30–45 (depends on supplier — Newhaven direct or distributor), rear LCD ~$15–25. Total displays ~$45–70 per unit. `bom-v1.md` update deferred to Justin's RF-modulator commit.
  - Files updated: `01-spec.md` Front panel + Rear panel + Still buffer thumbnail sections; `panel-layout.md` front-panel inventory + cutout specs in mechanical TODO + open-question resolution; `ui-menu.md` surface-owners table + thumbnail layout note + front-panel TFT bus open question. `bom-v1.md` skipped to avoid colliding with Justin's uncommitted RF section additions.
- 2026-05-11 (PM, RF MLP correction) — **50→75 Ω transformer swapped for resistive minimum-loss pad. Corrects an earlier spec error.**
  - **The error:** prior RF subsystem spec offered Mini-Circuits TC4-1W+ transformer as the dB-preserving alternative to a resistive pad for 50→75 Ω matching. **TC4-1W+ is a 1:4 impedance ratio transformer (50↔200 Ω), NOT 50↔75.** Same applies to MACOM MABAES0061 which Justin flagged as a cheaper substitute. Confirmed by datasheet re-read 2026-05-11 PM. Both parts are wrong for the application; swapping one for the other was substituting one mistake for a cheaper version of the same mistake.
  - **Why no commercial "1:4 transformer" does 50↔75:** the impedance ratio needed is 1.5 (75÷50), which means turns ratio √1.5 ≈ 1.22:1. That's not a standard manufacturing ratio. True 50↔75 transformers (Mini-Circuits TM2-43X+, TCM2-33X+ class) exist with internal 1:1.5 turns but cost $3–8 and aren't justified here.
  - **The right answer: resistive Minimum-Loss Pad (MLP).** Two 1% resistors: **43.2 Ω series + 86.6 Ω shunt** (off-the-shelf 43 Ω + 82 Ω 1% is close enough; 0.7% Z-mismatch is invisible at our level budget). **5.7 dB insertion loss** — mathematically optimal for a purely resistive 50↔75 Ω match. Broadband DC-to-GHz. No tuning, no drift, no parasitic resonances, $0.10 in passives.
  - **Why 5.7 dB doesn't matter for us:** ERA-3SM+ has ~22 dB gain. Target output is −40 to −30 dBm at the F-connector. CRT tuners accept −50 to −10 dBm. The pad loss is rounding error inside the available dB headroom. Trading the pad for a $3–8 transformer to save dB we don't need is solving a problem that doesn't exist.
  - **Trigger for the catch:** Justin questioned the TC4-1W+ price against the cheaper MABAES0061. Investigating the substitution exposed that both are 1:4 ratio, neither does 50↔75, and the original recommendation was wrong. Honest BOM review caught a real error.
  - **BOM impact:** RF subsystem per-unit drops $33 → $32. Rollup: Base unit ~$816 (was $817), Broadcast unit ~$848 (was $849). Bench-eval starter drops ~$75–80 → ~$65–70 (TC4-1W+ ×2 removed, resistor pair added).
  - **Open-question closure:** "50→75 Ω match: resistive pad vs TC4-1W+ transformer" is no longer an open bench-eval-phase question — the layout-phase decision is committed to the MLP.
  - **Doc operations:** Updated four docs to align on MLP language and the corrected totals — `docs/rf-modulator-subsystem.md` (architecture diagram, § 4 50→75 Ω match section, BOM table, bench bring-up checklist, open-questions list), `docs/bom-v1.md` (§ 7 RF chain bullet, bench eval parts list, section subtotal table, open BOM questions, V1 rollup table), `docs/01-spec.md` (RF modulator output section: architecture summary line, total BOM line, open-questions list, baked-in-vs-daughter-card BOM-delta figure), and this changelog entry.
- 2026-05-11 (PM, front-panel mezzanine architecture) — **Front panel becomes its own mezzanine board with RP2040 + BridgeTek BT817Q EVE 4. STM32H735IGT6 retires from V1 production spec.** Architectural pivot driven by the dimensional review of the NHD-2.9 panel datasheet, which revealed (a) the panel's "Driver / No Controller" architecture means it needs continuous parallel-RGB pixel data — no SPI-only path was actually viable for pixel data, and (b) the 4-wire SPI option is for init commands only. Combined with Justin's preference to make the front panel its own board with RP2040 as the local MCU, the decision flow landed on a dedicated graphics-controller chip + RP2040 + simple host link.
  - **New front-panel mezzanine BOM:**
    - **RP2040** + 12 MHz crystal + W25Q16 QSPI boot flash + decoupling — ~$3 (UI MCU; reads encoders + buttons via PIO/GPIO, drives front-panel LED column, sends EVE command lists over SPI, syncs UI state to Zynq PS over UART)
    - **BridgeTek BT817Q EVE 4** graphics controller + 12 MHz crystal + decoupling — ~$10–13 single qty, ~$8 at qty 100 (drives NHD-2.9 over 24-bit parallel RGB, holds 1 MB internal RAM_G frame buffer, handles all rendering autonomously from RP2040 command lists)
    - **NHD-2.9 backlight boost regulator** (TI TPS61040 class) — ~$0.50 (the panel's 6.0 V / 100 mA backlight needs a small boost from the 5 V / 12 V rail; can't drive from 3.3 V directly)
    - **Front-mezzanine ↔ main-carrier connector** — small ribbon, ~6 pins (UART TX/RX + 12 V + 5 V + GND + reserve), ~$3 / pair
  - **STM32H735IGT6 retires from V1 production spec.** Was the planned UI MCU on the main carrier. Replaced by RP2040 + BT817Q on the front-panel mezzanine. **STM32H735G-DK already procured stays in inventory as a bench spare / educational asset only** (no longer in V1 production path).
  - **Two RP2040s total in V1:** one on the main carrier for genlock slow-control (previously banked), one on the front-panel mezzanine for UI duties. Different roles, different boards, no shared firmware, no interference. Bare RP2040 chips ~$1 each — negligible BOM impact.
  - **UI framework simplifies dramatically.** No TouchGFX, no LVGL, no LTDC parallel routing. BridgeTek EVE Asset Builder / EVE Screen Editor + the FT8xx command set is a higher-level paradigm: RP2040 sends display lists ("draw rect, draw text, load JPEG, set brightness"), EVE renders to RAM_G, drives the panel. Built-in primitives include rectangles, lines, anti-aliased text, JPEG/PNG decode, button widgets, gauges, sliders, screen rotation, audio synthesis.
  - **Front-panel TFT drive path correction:** the prior changelog note that the NHD-2.9 could be driven over SPI directly from the STM32H735 was wrong — the SPI on this panel is for init only; pixel data must come over parallel RGB. BT817Q EVE chip resolves this by holding the frame buffer internally and driving parallel RGB to the panel.
  - **Rear LCD (NHD-1.5) detail updates from datasheet review:** has built-in controller + frame RAM (write-once, hold-forever — perfect for 1 Hz status updates). Interface mode hardwired to 4-wire SPI via strap pins IM0=0, IM1=1, IM2=1. 3.0 V / 100 mA backlight needs a small dedicated regulator (LDO from 3.3 V is sufficient, ~$0.20). 1200 cd/m² brightness. Owned by Zynq PS over one dedicated SPI port; remains a carrier-board element (not mezzanine).
  - **Mezzanine ↔ carrier link is UART + power only.** No SPI, no I²C, no parallel buses cross the mezzanine connector. Cleaner cable, smaller connector, no high-speed signal integrity concerns.
  - **Boot time:** UI alive in < 1 s from cold boot via RP2040 (no Linux dependency). Main system can take 15-30 s to boot Linux on Zynq PS behind the scenes with progress bar shown on the front TFT.
  - **Bench eval kit additions needed:** Raspberry Pi Pico (RP2040 dev board, ~$4) + a BT817Q dev board (Crystalfontz CFA10110 or Riverdi RVT-EVE-eval, ~$80–150) for front-panel firmware development.
  - **Files updated:** `01-spec.md` (Hardware foundation UI MCU bullet, Development hardware section, Front panel TFT bullet, UI architecture section, Rear panel status display section with backlight + strap-pin details); `ui-menu.md` (surface-owners table + front-panel TFT bus open-question resolution); `panel-layout.md` (front-panel inventory adds RP2040+BT817Q row, TFT description updated to reflect new drive path); `signal-flow.md` diagram 3 control plane (STM32H735 block replaced with `UIMEZ` mezzanine subgraph containing RP2040 + BT817Q + the panel-side elements, wires updated to show RP2040 → SPI → BT817Q → parallel RGB → TFT chain, UART link to Zynq PS state aggregator, notes section); this changelog. `bom-v1.md` not touched — Justin's RF-modulator additions are unstaged there; he'll roll in the mezzanine BOM line when finalizing.
- 2026-05-13 — **Active dev priority shift: HD pipeline first, analog terminal encoders last.** Justin's call: skip chroma burst bench verification for now (Phase 2 finishing work); focus on building the HD pipeline top-down (HDMI in/out → VDMA → scaler → FRC → color → geometry → re-attach analog terminal encoders at the end). Active dev platform stays the **Digilent Zybo Z7-20** (same Z-7020 silicon family as production TE0720; HDL ports 1:1). The chroma burst, I/Q chroma modulator, R2R amplitude calibration, and colored test patterns work all become Phase G work — paused, not abandoned. New phase tasks A–G filed in TaskList.
- 2026-05-13 — **Doc tree consolidation: one internal-electronics spec, two packaging variants.** Justin's call: Mini and Pro share the same carrier PCB design and TE0720 SOM. SKU split is packaging-only (chassis form factor + front-panel hardware + rear-panel I/O complement + carrier stuffing variant). Internal architecture is one design.
  - **Merged into a single canonical `01-spec.md`:** prior `01-spec.md` (Pro-tier full spec) + `mini-spec.md` (Mini scope) + `carrier-board.md` (production carrier design). New `01-spec.md` covers the unified electronics architecture with `[All SKUs]` / `[Pro only]` / `[stuffing variant]` annotations per subsystem.
  - **New `packaging-skus.md`** captures everything SKU-specific: chassis form factor, front-panel hardware (Mini = mono OLED + tactile; Pro = NHD-2.9 + mezzanine), rear-panel I/O complement, carrier stuffing matrix, per-SKU BOM rollups, open packaging decisions (Mini WiFi chip, Mini PSU strategy, Mini S-Video connector).
  - **New `dev-roadmap.md`** = SSOT for active dev arc + recently-completed work + deferred work + forward-looking hardware migration + forward-looking SKU build-out. Prevents the doc-misuse trap where forward-looking architecture docs get mistaken for active dev state.
  - **Deleted:** `mini-spec.md`, `mini-phase1-bringup.md`, `carrier-board.md` (absorbed into `01-spec.md` + `packaging-skus.md` + `dev-roadmap.md`); `rf-modulator-daughter-card.md` (redirect stub from when RF moved from daughter-card to built-in); `02-features-backlogREPLACED BY 01-SPEC.md` (already marked stale); `03-chroma-implementation.md` (legacy Phase 2 chroma detail — content folded into `01-spec.md` § 2.3 + `dev-roadmap.md` Phase G).
  - **Decisions banked during merge:**
    - **RF modulator: Pro-tier only** (Mini stuffing omits ADL5391 + dedicated Si5351 + ERA-3 + bandpass + shield can + F-connector). Period CRT support is a Pro feature.
    - **Genlock: both SKUs.** Mini gets the RP2040 + Si5351 + LTC6912 + AD9204 genlock front-end (1 BNC REF IN), no dual SYNC OUT driver chain (Pro adds that).
    - **Pro mezzanine UI (RP2040 + BT817Q + NHD-2.9 + NHD-1.5 + per-connector LEDs + rear status LCD): Pro-only.** Mini uses simpler 1.3" mono OLED + 9 tactile switches driven directly by Zynq PS via `i2c-dev` + `gpio-sysfs`.
    - **Stuffing-variant approach over daughter-card-on-headers** for SDI silicon — Pro factory-stuffs GS3470 + GS2962; Mini doesn't. Cleaner manufacturing than headers-and-daughter-card. (The expansion header design rule remains for future add-on capability, not SDI-specifically.)
  - **TE0701 references removed from spec docs.** TE0701 was a mistaken entry in yesterday's docs; never actually procured (only TE0703-07 was). All non-TE0720 hardware references (Smart Artix, Smart Zynq SL, TE0712, TinyZynq, TE0703, TE0720-04-31C33MA, TE0720-04-61C530A, Arty S7-25) are scoped to `dev-roadmap.md` § 6 (future-evaluation candidates) and `00-index.md` "future-evaluation hardware" section. Active dev platform is **Zybo Z7-20**; production target is **TE0720-04-62I33MA**.
  - **`bom-v1.md`** lightly updated (status header + SKU scope) but content preserved — Justin has unstaged RF additions there that he'll commit separately.
  - **Light SKU-scope notes** added to: `signal-flow.md` (Pro-architecture diagrams, Mini differences noted), `panel-layout.md` (Pro panel; Mini in packaging-skus.md), `ui-menu.md` (Pro UI; Mini UI deferred), `mvphd-comparison.md` (Schindler 2.0 in comparison = Pro feature set).
  - **`00-index.md`** rewritten to reflect the new doc tree and SKU strategy.
- 2026-05-15 — **Inbound hardware logged + Pro mezzanine dev platform decided + VGA OUT moved from killed to under consideration.** Three discrete updates batched into one banking pass while filesystem MCP was intermittently disconnected.
  - **Inbound 2026-05-13 hardware section added to `00-index.md`.** Two orders logged:
    - *Digilent (FedEx 487403305746):* Pmod OLED 128×32 mono (410-222), Pmod OLEDrgb 96×64 color (410-323), Pmod ENC rotary encoder ×2 (410-117), Pmod SWT 4-switch (410-083), Pmod R2R (410-165), Pmod TPH2 test points (410-135), Pmod Cable Kit + splitter (240-021-2), Pmod VGA (410-345). Aimed at Arty S7-25 ecosystem for parallel front-panel UI prototyping + (conditional) VGA OUT prototyping if banked.
    - *HelloFPGA (China):* Smart Artix V1.3 + Smart Zynq SL V1.3 (inventory only — already archived in `docs/Hardware/`), LT8619C HDMI-to-RGB chips ×5 (production-quantity HDMI-input silicon stockpile), LT8619C demo board (second eval).
    - Net effect: enables a parallel front-panel UI prototyping arc on Arty S7-25 + Pmods that doesn't block on Zybo HD pipeline phases A-G, plus production-quantity HDMI-input silicon on hand.
  - **Pro mezzanine EVE dev platform: Riverdi RVT43HLBFWN00** (4.3" 480×272 EVE4 intelligent display with BT817Q + flash + boost regulator + everything on a single board, ~$62). Selected over the previously considered BridgeTek ME817EV evaluation board (~$100–120) for three reasons: (a) **interface match** — ME817EV's 50-pin RGB connector and 40-pin LVDS connector don't pin-match the production NHD-2.9 panel's 40-pin FFC (different pitch, different pin order), would require custom adapter PCB to actually drive NHD-2.9; (b) **complete out of the box** — RVT43HLBFWN00 is an intelligent display, so EVE chip + LCD + flash + backlight regulator all integrated and ready to send SPI command lists from a Pico host; no adapter PCB to fab, no level shifters to wire; (c) **matches production silicon exactly** — BT817Q (EVE4) is what gets soldered onto the production Pro mezzanine. Different resolution and aspect ratio than NHD-2.9 (480×272 landscape vs 376×960 portrait-rotated-landscape) is acceptable since EVE programming model is panel-agnostic and the resolution can be re-tuned at deployment. Order pending at Mouser (DigiKey out of stock; Riverdi direct $100 shipping rejected). Banked into `dev-roadmap.md` § 5.2 as Pro mezzanine dev hardware and noted in `00-index.md` inbound section. **Phase ordering:** RVT43HLBFWN00 supports EVE toolchain learning + Pro UI flow prototyping in parallel with Zybo HD pipeline work; NHD-2.9 production panel validation deferred to mezzanine schematic phase (later) via custom adapter board.
  - **EVE generation evaluation:** Bridgetek launched BT820B (EVE5) March 2025. Considered as an upgrade path for the Pro mezzanine, ruled out for V1: (a) BT820B outputs LVDS only — not parallel RGB — so it can't drive the NHD-2.9 (parallel RGB panel) without an LVDS-to-RGB converter; (b) 329-ball LFBGA package vs BT817Q's VQFN64 adds X-ray inspection + more complex assembly partner; (c) external DDR3 SDRAM required vs BT817Q's internal 1 MB RAM_G; (d) targeted at 1920×1200 industrial HMI panels with live video blending — overkill for a 376×960 status display. BT817AQ (refinement of BT817Q, pin-compatible, current production variant) noted as a minor variant worth tracking for production BOM but not requiring spec change at this time. **Decision: stay with BT817Q on production mezzanine.**
  - **VGA OUT moved from killed (§ 22) to under consideration (§ 21).** Earlier `01-spec.md` § 22 row "VGA in/out (HD-15) | Killed by design..." replaced with narrower "VGA IN (HD-15) | Significant HDL complexity (auto-positioning + sample-phase recovery) for a declining input format. Out of scope for V1." — i.e., only VGA IN remains killed; VGA OUT promoted to open-question status.
    - **Use case driving reconsideration:** driving VGA-input CRTs on camera at 24/30 fps film cadences. Period sets and modern shoots where a VGA computer monitor needs to be visible on-camera showing source content at the camera's frame rate. Most VGA monitors won't lock below ~31 kHz horizontal sync, so raw 24/30 Hz output won't display — the solution is frame-doubling to 48/60 Hz refresh (24p → 48 Hz, 30p → 60 Hz), which gives VGA-monitor-compatible refresh rates while keeping integer camera-shutter / CRT-refresh alignment.
    - **Architecture sketched (pending bank-decision):** ADV7125BCPZ-AAAA triple 8-bit video DAC (330 MSPS, ~$6) + 74AHCT125 sync level shifter (~$0.50) + HD-15 panel-mount connector (~$2) + 75 Ω termination passives (~$1.50) → ~$10/unit BOM addition. Both SKUs. Drives a dedicated rear-panel HD-15 with R/G/B analog + HSYNC/VSYNC. Independent of the ADV7393 analog terminal — runs from its own DAC chain off the HD signal bus so it can output at VGA-compliant refresh rates while the CRT analog terminal runs at native 24/30 fps.
    - **HDL implications (pending bank-decision):** new VGA terminal encoder block in the FPGA. Taps HD signal bus → 1:2 frame doubler → VGA-rate pixel timing generator → RGB to ADV7125 + HSYNC/VSYNC through level shifter. Operator selects output resolution (640×480 / 800×600 / 1024×768 / 1280×720) and encoder derives refresh rate from source cadence. New HDL effort — not absorbed by existing blocks. Bench prototype path: Digilent Pmod VGA (410-345, on inbound) provides R-2R DAC + HD-15 on Zybo Z7-20 for HDL block bring-up before committing to ADV7125 on production carrier.
    - **Why this isn't banked yet:** Justin wants VGA OUT off the killed list but explicitly not yet a confirmed feature — only under consideration. So this changelog entry captures the analysis done in case the decision flips to YES later, but the spec docs (§ 3 subsystems, § 4.2 output rates, § 16 rear panels, packaging-skus stuffing matrix, dev-roadmap Phase G) are NOT modified to assume VGA OUT will ship. Only § 21 open questions carries the VGA OUT under-consideration entry with the architecture summary. **Pending Justin's call.**
  - **Files updated:** `00-index.md` (Inbound section added, Pmod VGA note flagged as conditional on banking decision); `01-spec.md` (§ 21 adds VGA OUT under consideration entry, § 22 narrowed to kill only VGA IN); `packaging-skus.md` (no content change — considered adding ME817EV reference but decision pivoted to RVT43HLBFWN00 which lives in `dev-roadmap.md`); `dev-roadmap.md` (§ 5.2 Pro mezzanine adds RVT43HLBFWN00 as EVE dev hardware with rationale).
  - **Working-style note (process correction):** initial banking pass over-committed VGA OUT as a confirmed feature — added full § 3.10 subsystem detail, § 4.2 output rates, § 16 HD-15 rows, packaging-skus VGA stuffing row + BOM additions, and Phase G HDL block in dev-roadmap. Justin's reply clarified: "VGA is just off the NO list and back to under consideration for both Mini and Pro in the same format, as described." Rolled back the over-commit; only § 21 open-question entry remains in spec docs proper, full architecture detail preserved here in changelog for future reference. Confirms working-style preference: don't auto-rebuild scope based on inferred sign-off; require explicit decision-to-bank.
