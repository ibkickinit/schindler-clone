pytest>=8.0
pytest-asyncio>=0.23
pyserial>=3.5
websockets>=12.0
jsonschema>=4.0

# Optional — Playwright web smoke (make web-smoke). Skipped if not installed.
# Install: pip install playwright && playwright install chromium
# playwright>=1.40
